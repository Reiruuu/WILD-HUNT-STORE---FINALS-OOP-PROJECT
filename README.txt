
To compile the program properly.

    Step 1 : Create a folder, Anywhere you actually want.
    Step 2 : Compile/Put all the files (source file, headers, and savefile.txt) inside that said folder.
    Step 3 : Right click an empty space in the file explorer, and click "Open in terminal"
    Step 4 : Once inside the terminal, type in " g++  main.cpp -o WildHunt.exe" to compile.
    Step 5 : You should now have a "main.exe" file in your folder. Now type in the terminal "./WildHunt.exe" or "WildHunt.exe" to run the file.


if Terminal cant find g++
    

    ASSSUMING UR INSIDE THE TERMINAL
    Step 1 : $env:Path += "Your g++ path. example for codeblocks: C:\Program Files\CodeBlocks\MinGW\bin"
    Step 2 : g++  main.cpp -o WildHunt.exe
    Step 3 : ./WildHunt.exe / "WildHunt.exe"

